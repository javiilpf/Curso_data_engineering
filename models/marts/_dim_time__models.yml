version: 2

models:
  - name: dim_date
    description: Spina temporal diaria para MetricFlow.

    time_spine:
      standard_granularity_column: date_day

    columns:
      - name: date_day
        granularity: day