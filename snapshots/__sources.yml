version: 2

sources:

  - name: GOOGLE_SHEETS # name of the source 
    schema: GOOGLE_SHEETS # this is the schema our raw data lives in
    database: DB31_BRONZE_DB # this is the name of our database

    tables:

      - name: users
        columns:
          - name: Nombre
          - name: DNI
          - name: email
          - name: fecha_alta_sistema